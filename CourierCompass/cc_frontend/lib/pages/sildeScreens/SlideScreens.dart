import 'package:flutter/material.dart';

class SlideScreens extends StatelessWidget {
  const SlideScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
    );
  }
}